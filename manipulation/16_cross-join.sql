-- cross join
-- join two or more tables
-- include all rows from both tables
-- each row is the combination of the row in the first table with the row in the second table
-- n x m rows

--  SELECT * FROM t1
--  CROSS JOIN t2;

--  SELECT
--    store_name,
--    product_name
--  FROM
--    stores AS a
--    CROSS JOIN products AS b;
